language: rust
rust:
  - stable
  - nightly
jobs:
  allow_failures:
    - rust: nightly
  fast_finish: true
cache: cargo

script:
    - cargo build --verbose
    - cargo test --verbose

notifications:
  email:
    recipients:
      - scott@rerobots.net
    on_success: change
    on_failure: always
