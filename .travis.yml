language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

install:
  - pip install -r py-ws/devel-requirements.txt
  - cd py-ws && pip install . && cd ..

script: make check

notifications:
  email: false